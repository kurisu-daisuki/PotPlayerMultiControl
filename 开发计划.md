# 开发计划

本文档记录后续功能规划，按优先级排序。实现时可再拆成独立 PR。

---

## 1. 一键对齐进度（目标：帧级对齐 + 播放中零漂移）

### 目标
1. **一键对齐**：将当前发现的所有 PotPlayer 窗口对齐到约定的播放位置，**以帧级对齐为最终目标**（含 **120fps**，约 **8.33ms/帧**）。
2. **帧差补偿**：支持手动设定各视频相对基准的**帧偏移**（例如「A 比 B 快 240 帧」），对齐时自动换算进目标时间。
3. **播放中长期零漂移**：对齐后在继续播放过程中，通过周期监测与微调，尽量保持各窗口相对进度（含帧差）长期不拉开。

接口层：PotPlayer `0x5004`/`0x5005` 单位为 **毫秒**，时间戳分辨率（1ms）小于 120fps 一帧，具备冲击帧级的前提。关键帧 seek、解码缓冲、多实例时钟漂移仍是主要挑战，需分阶段逼近并实测。

### 建议行为

**对齐**
- 工具栏新增「对齐进度」按钮（及对应快捷键，见第 2 项）。
- 默认策略：以**主窗口**（默认列表首个有效窗口，或用户指定）的进度为基准；其余窗口按「基准时间 + 该窗口帧偏移」seek。
- 对齐前建议先统一为**暂停**，再 seek，再回读校验。

**帧差（帧偏移）**
- 用户可为每个已发现窗口（或相对主窗口的配对）手动设定**帧偏移**，单位为帧，可正可负。
  - 约定示例：若 **A 比 B 快 240 帧**（同一时刻 A 的画面内容更靠后），则相对主窗口 B 时，A 的偏移为 `+240`（对齐时 A 的目标时间 = 主窗口时间 + 240/fps）。
  - 符号与 UI 文案需写死一种约定，避免「快/慢」歧义（建议界面同时显示「相对主窗口：±N 帧」）。
- 换算：`offsetMs = frameOffset * 1000 / fps`（优先整数运算）；`fps` 可全局配置（24 / 30 / 60 / 120 等）或按窗口配置。
- 帧差设置需**持久化**（与现有 seek 秒数设置类似，存本地），刷新窗口列表后尽量按窗口句柄 / 标题 / 进程匹配恢复。

**播放中零漂移**
- 对齐成功后可开启「同步锁」（默认开启或可切换）：定时读取各窗口进度，若相对主窗口（含帧差）偏差超过阈值（如 ≥1 帧），则对偏离窗口微调 `SetCurrentTime`。
- 播放态与暂停态均可监测；播放中微调注意冷却，避免频繁 seek 卡顿。
- 某窗口控制失败（UIPI 等）时跳过并提示，不拖垮整组同步。

### 实现要点（分阶段）

**阶段 A — 毫秒对齐（MVP）**
- 复用 `PotGetCurrentTime`（`0x5004`）、`PotSetCurrentTime`（`0x5005`），见 `MainForm.TrySendSeek`。
- 流程：`FindAll()` → 读基准时间 → 全部 `SetCurrentTime`（钳制到 `0..total`）→ 处理 UIPI/提权提示。
- UI：对齐按钮 + 基础状态反馈。

**阶段 B — 帧级对齐 + 手动帧差**
- 按配置帧率将目标时间量化到帧边界。
- UI：每窗口（或相对主窗口）可编辑「帧偏移」；对齐时应用 `targetMs = baseMs + offsetMs`，再帧边界量化。
- 对齐后回读校验；超阈值短延迟重试 1～2 次。
- 探索 PotPlayer 更精确的逐帧 / 精确 seek（若有则优先使用）。

**阶段 C — 播放中长期零漂移**
- 后台定时器（如 200–500ms，可配置）轮询各窗口进度 vs 期望进度（主窗口 ± 帧差）。
- 偏差 ≥ 阈值则微调；记录最大偏差与微调次数到日志。
- 切换影片 / 窗口列表变化时重置期望关系；用户关闭「同步锁」则停止。
- 验收以「同文件或已设正确帧差、同 fps」为主；不同时钟源下的极限能力需实测并文档化。

### 验收

**阶段 A**
- [ ] 两窗口及以上进度不一致时，点击后时间戳接近同一毫秒目标。
- [ ] 无窗口 / 仅一个窗口时有明确状态提示，不崩溃。
- [ ] 管理员窗口无法控制时提示与现有命令一致。

**阶段 B（帧级 + 帧差）**
- [ ] 同文件、暂停、120fps 条件下，对齐后回读误差 **≤ 1 帧**。
- [ ] 设定「A 相对 B = +240 帧」后，对齐结果满足 A 的进度 ≈ B + 240/fps（误差 ≤ 1 帧）。
- [ ] 帧差与假定帧率可保存，重启后仍可用。
- [ ] 状态栏或日志显示对齐结果（成功数、最大误差 ms / 帧）。

**阶段 C（零漂移）**
- [ ] 对齐并开启同步锁后，连续播放数分钟，相对进度（含帧差）持续维持在阈值内（目标 ≤ 1 帧；若硬件/播放器限制则记录实测上限）。
- [ ] 关闭同步锁后不再自动微调。
- [ ] 微调不过度频繁，不明显拖垮 UI / 播放流畅度。

---

## 2. 快捷键改为非全局、单手可操控

### 目标
- **非全局**：取消 `RegisterHotKey` 系统级热键；仅在本程序窗口获得焦点时响应按键，避免与其他软件冲突。
- **单手可操控**：按键布局集中在一只手可及范围（建议左手），无需左右手配合 `Ctrl+Alt + HJKL`。

### 建议键位（草案，实现前可微调）

| 功能       | 建议按键 | 说明        |
|------------|----------|-------------|
| 后退       | `A`      | 左手        |
| 播放/暂停  | `S`      | 左手        |
| 快进       | `D`      | 左手        |
| 回到起始点 | `Q`      | 左手上方    |
| 对齐进度   | `W`      | 与第 1 项配套 |
| 显示全部   | `E`      | 左手上方    |
| 最小化全部 | `R`      | 左手上方    |

也可改为数字小键盘区（单手右手）；最终以「一只手、少修饰键、不抢系统全局」为准。推荐默认 **无修饰键**（窗体焦点内直接按字母）。

### 实现要点
- 移除 / 停用：`RegisterHotKey` / `UnregisterHotKey` 及 `WM_HOTKEY` 分支。
- 改用窗体级处理，例如：
  - 重写 `ProcessCmdKey`，或
  - `KeyPreview = true` + `KeyDown`；
- 仅在本窗体激活且焦点不在「时间跨度 / 帧偏移」等输入控件时拦截字母键。
- 更新按钮 tooltip、状态日志中的热键文案；同步更新 `README.md`。
- 控制窗口建议默认或易用「置顶」，方便切回本程序后单手操作。

### 验收
- [ ] 本程序未聚焦时，按上述键不影响其他应用。
- [ ] 本程序聚焦时，单手可完成播放控制与（实现后）对齐进度。
- [ ] 在秒数 / 帧偏移等输入框编辑时，字母键不被误当成命令。
- [ ] README 与界面提示文案与实际键位一致。

---

## 实施顺序建议

1. **非全局快捷键**
2. **对齐 · 阶段 A**（毫秒对齐 + 按钮/快捷键）
3. **对齐 · 阶段 B**（帧级量化 + 手动帧差 + 持久化）
4. **对齐 · 阶段 C**（播放中同步锁 / 长期零漂移）
5. 更新 README 与本计划验收勾选

## 非目标（本阶段不做）

- 完整自定义快捷键配置界面（帧差/帧率用简单控件即可，不做通用热键编辑器）
- 跨机器同步进度
- 自动检测两视频内容差异并估算帧差（帧差由用户手动设定）
- 全局热键与窗体内热键双模式并存开关（若后续有需求可再开）
